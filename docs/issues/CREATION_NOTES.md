# Issue Creation Notes

GitHub issue creation was requested, but this environment does not have GitHub CLI (`gh`) or API credentials configured.

Prepared issue drafts in this folder can be copied directly into GitHub Issues in priority order.
